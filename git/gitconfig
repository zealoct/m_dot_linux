[user]
	name = hanjinchen
	email = hanjinchen@baidu.com
[core]
        editor = emacs -nw
[color]
        status = auto
        branch = auto
        diff = auto
        ui = true
        pager = true
[color "branch"]
        current = normal reverse
        local = normal
        remote = red
[color "diff"]
        meta = yellow bold reverse
        frag = magenta bold reverse
        old = red bold
        new = green bold
[color "status"]
        added = green
        changed = red
        untracked = cyan
[http]
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
